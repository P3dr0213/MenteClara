# Criado automaticamente por npm run db:setup. Nunca colocar estas credenciais no app.
DATABASE_URL=postgresql://mente_clara_app:SUA_SENHA@127.0.0.1:5432/mente_clara
# Gere com: node -e "console.log(require('crypto').randomBytes(48).toString('hex'))"
JWT_SECRET=SUBSTITUA_POR_UM_SEGREDO_ALEATORIO
HOST=127.0.0.1
PORT=3001
